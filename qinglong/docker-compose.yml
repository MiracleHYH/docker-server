version: '3'

services:
  qinglong:
    image: whyour/qinglong:latest
    container_name: qinglong
    volumes:
      - ./data:/ql/data
    ports:
      - 5700:5700
    deploy:
      resources:
        limits:
          memory: 1G
    restart: unless-stopped